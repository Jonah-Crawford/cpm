# cpm
## How to install and use

- Install Node.JS, this contains the npm package manager.
- Run `npm install` to install the dependencies.
- For local development, run `npm run watch:css`, and `cargo run` to start the server. This looks for changes in CSS.
- For Prod run `npm run build:css` to build the CSS.
